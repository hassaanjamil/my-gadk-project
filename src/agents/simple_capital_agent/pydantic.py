# from pydantic import BaseModel, Field

# class CapitalOutput(BaseModel):
#     """Structured response containing a country's capital."""
#     capital: str = Field(description="The capital city for the requested country.")
